export interface ICategory {
  id: number;
  title: string;
  description: string;
  image?: string;
  color?: string;
}

export interface IPlaylist {
  id: number;
  name: string;
  description: string;
  image?: string;
  color?: string;
}
